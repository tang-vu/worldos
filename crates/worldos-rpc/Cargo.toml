[package]
name = "worldos-rpc"
description = "WorldOS JSON-RPC dispatch layer: stdio endpoint, WebSocket server, MCP server"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
worldos-kernel = { path = "../worldos-kernel" }
worldos-commands = { path = "../worldos-commands" }
worldos-engine = { path = "../worldos-engine" }
worldos-agent = { path = "../worldos-agent" }
worldos-capability = { path = "../worldos-capability" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
tungstenite.workspace = true
ulid.workspace = true
